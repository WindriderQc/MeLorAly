<div class="container-fluid vh-100 d-flex align-items-center justify-content-center">
    <div class="text-center">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="error-template">
                    <h1 class="display-1 text-danger">500</h1>
                    <h2 class="mb-4">Erreur serveur</h2>
                    <div class="error-details mb-4">
                        <p class="text-muted">
                            Une erreur interne s'est produite. Notre équipe a été automatiquement notifiée 
                            et travaille à résoudre le problème.
                        </p>
                        <% if (process.env.NODE_ENV !== 'production') { %>
                            <div class="alert alert-warning text-start mt-3">
                                <small><strong>Mode développement:</strong> Vérifiez la console pour plus de détails.</small>
                            </div>
                        <% } %>
                    </div>
                    <div class="error-actions">
                        <button onclick="window.history.back()" class="btn btn-outline-secondary me-2">
                            <i class="bi bi-arrow-left me-2"></i>Retour
                        </button>
                        <a href="/" class="btn btn-primary me-2">
                            <i class="bi bi-house-door me-2"></i>Retour à l'accueil
                        </a>
                        <% if (user) { %>
                            <a href="/dashboard" class="btn btn-outline-primary">
                                <i class="bi bi-speedometer2 me-2"></i>Tableau de bord
                            </a>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>